import math
class Solution:
    # @return a string
    def convert(self, s, nRows):
    	l = len(s)
        lower = l/2.0/(n-1)
        # upper = (l-3+2n)/2.0/(n-1)
        x = math.ceil(lower)
        l = 
        r = ''
        for i in range(l):
        	r[i] = ''

